# Local development overrides on top of the default H2 profile.
# Enables the H2 web console at /h2-console for inspecting the in-memory DB.
# Never enable this in production.
spring.h2.console.enabled=true
